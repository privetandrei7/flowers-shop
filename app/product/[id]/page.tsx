'use client';
import { notFound, useParams } from 'next/navigation';
import Link from 'next/link'; import Header from '@/components/Header'; import { products, formatPrice } from '@/data/products'; import { useCart } from '@/components/CartProvider';
export default function ProductPage(){ const {id}=useParams<{id:string}>(); const p=products.find(x=>x.id===id); const {add}=useCart(); if(!p) return notFound(); return <><Header/><main className="container product-page"><div className="breadcrumb"><Link href="/">Главная</Link> / <Link href="/catalog">Каталог</Link> / {p.name}</div><div className="product-detail"><div><img src={p.image} alt={p.name}/></div><div className="product-info"><span className="eyebrow">LUMIÈRE FLOWERS</span><h1>{p.name}</h1><div className="price-big">{formatPrice(p.price)} {p.oldPrice&&<del>{formatPrice(p.oldPrice)}</del>}</div><p className="lead">{p.description}</p><div className="detail-box"><b>Состав</b><span>{p.composition}</span></div><div className="detail-box"><b>Доставка</b><span>Сегодня по Киеву · от 150 ₴</span></div><button className="primary-btn" onClick={()=>add(p)}>Добавить в корзину</button><Link className="secondary-btn" href="/checkout">Перейти к оформлению</Link></div></div></main></> }
